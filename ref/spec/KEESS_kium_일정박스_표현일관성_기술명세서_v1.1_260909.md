# KEESS `/kium` 일정 박스 표현 일관성 기술명세서 v1.1

- **작성일**: 2026-09-09 (v1.0 260908 개정)
- **개정 사유**: 사업 검토 영상(`마감도간략히보기카드일관되게.mp4`) 재분석 결과 v1.0이 **액션 영역의 형태 불일치**를 다루지 않아, F27~F29를 모두 반영해도 마감 카드만 액션 모양이 달라지는 문제가 남는 것을 확인
- **대상 저장소**: `KEESS_pedu`
- **대상 화면**: 과정안내 탭 > 공개교육 보기 > 일정 박스(`.kium-schedbox`)
- **기능 ID**: **F27 · F28 · F29 · F30 · F31 · F32** (F24~F26에서 이어짐)
- **선행 문서**: `ref/kium/spec/KEESS_kium_마감회차_노출개선_기술명세서_v1.0_260908.md`(F24~F26) · `ref/kium/spec/KEESS_kium_마감시드_EmptyCase검토칩_기술명세서_v1.0_260907.md`(F21~F23)
- **문서 지위**: 본 건 구현의 **단일 기준**. **F24(스트립 0장 폴백)를 폐기하고 대체**한다. F21의 스트립 `closed` 제외 규칙(F0-9)과 검증 M1도 본 문서로 **폐기**한다.

---

## 1. 제기된 문제 (사업 검토, 2026-09-08)

> "모집중·개강확정·마감임박의 「간략히 보기」 상태 UI와 마감일 때의 UI를 왜 다르게 하나? 이 상태면 마감에서는 「간략히 보기」로 전환해도 UI가 동일해서 '뭐가 바뀐 거야? 오류 아니야?' 할 수 있지 않나?"

### 1-1. 실측 재현 (배포본, 2026-09-08)

| 조작 | 토글 라벨 | `.kium-scard2`(카드) | `.kium-srow`(리스트 행) | 화면 |
| --- | --- | :--: | :--: | --- |
| 모집 상태 `전체` | `전체 일정 20개 회차` | **6** | 0 | 스트립 |
| `마감 1` 선택 | `전체 일정 1개 회차` | **0** | **1** | 리스트 |
| 위에서 토글 1회 클릭 | `간략히 보기` | 0 | 1 | **동일** |
| 토글 1회 더 클릭 | `전체 일정 1개 회차` | 0 | 1 | **동일** |

### 1-1-2. 사업 검토 영상 분석 (`마감도간략히보기카드일관되게.mp4`, 5.9초)

| 시점 | 조작 | 화면 |
| --- | --- | --- |
| 0.0~2.0s | `마감임박 5` 선택 상태 | **카드 5장**이 가로 스트립으로 노출 |
| 2.5s | `마감 1` 클릭 직후 | 일정 박스 본문이 **빈 공간** (전환 중) |
| 3.0~6.0s | `마감 1` 선택 완료 | **가로 행 1건**(리스트 행)이 노출. 카드 아님 |
| 5.0s | 토글 `간략히 보기` 클릭 | 라벨만 바뀌고 **행 그대로** |

파일명이 요구사항을 그대로 담고 있다: **「마감도 간략히 보기 카드 일관되게」**. 즉 요구는 「간략히 보기 상태에서 회차 하나하나의 UI가 모집 상태와 무관하게 동일한 형태여야 한다」이다.

### 1-2. 문제의 정체 (4건)

**P1. 같은 축에서 값만 바꿨는데 표현 문법이 바뀐다**

「모집 상태」는 **데이터 값을 고르는 축**이다. `개강확정`을 고르면 카드 스트립, `마감`을 고르면 세로 리스트가 나온다면, 사용자는 자기가 고른 것이 값인지 화면 모드인지 알 수 없다. 필터는 목록을 거르는 것이지 **화면 문법을 바꾸는 컨트롤이 아니다.**

**P2. 토글이 죽은 컨트롤이 된다**

`마감` 상태에서 토글은 노출되고 라벨도 바뀌지만 **화면은 그대로다.** 눌러도 아무 일이 없는 컨트롤은 사용자에게 오류로 읽힌다. 검수자에게는 결함 리포트 대상이 된다.

**P3. Empty Case 검토 칩이 사실상 미반영 상태다**

F23으로 `Empty Case` 칩을 넣었으나 `?preview` 쿼리 게이트 뒤에 있다. 실측 결과 `?preview=cases`에서만 `.kium-chip-review` 1개가 렌더되고, 일반 URL에서는 DOM에 존재하지 않는다. 이 배포본은 **디자이너·개발자가 보는 기획서**인데, 쿼리를 아는 사람만 볼 수 있으면 "반영되지 않았다"로 인지된다. 실제로 사업 검토에서 그렇게 보고되었다.

**P4. 액션 영역의 형태가 상태에 따라 다르다 (v1.0이 놓친 지점)**

실코드 대조 결과, 회차 액션이 **두 가지 서로 다른 구조**로 존재한다.

```html
<!-- 신청 가능 상태 (카드·리스트 공통) : 단일 pill 버튼 -->
<button class="kium-sact" data-tone="red">
  <span class="kium-sact-st"><svg/><span class="kium-sact-lb">마감임박</span></span>
  <span class="kium-sact-go">상담하기<svg/></span>
</button>

<!-- 마감 상태 (리스트 전용) : div + 정적 배지 + 텍스트 링크 -->
<div class="kium-sact-closed">
  <span class="kium-sbadge" data-tone="gray"><svg/><span>마감</span></span>
  <button class="kium-cta-next"><svg/><span>다음 회차 상담</span></button>
</div>
```

컨테이너를 카드로 통일해도(F27~F29) **이 액션 구조를 그대로 옮기면 마감 카드만 액션 모양이 다르다.** 불일치가 한 단계 안으로 옮겨갈 뿐이며, 같은 지적이 다시 제기된다.

### 1-3. 원인

F24가 「스트립이 0장이면 그 자리에 리스트를 렌더」하는 **폴백**으로 문제를 우회했기 때문이다. 일정 박스는 원래 **표현 모드 2종**(간략히 = 스트립 / 전체 일정 = 리스트)을 토글로 오가는 구조인데, 폴백이 그 위에 **데이터 조건에 따른 세 번째 상태**를 끼워 넣어 토글과 충돌했다.

**근본 원인 2가지**

1. 스트립이 `closed`를 표현하지 못한다는 제약(F0-9). 폴백은 그 제약을 우회한 것이다
2. 마감 액션이 **리스트 전용 구조로 별도 설계**되어 공용 액션 컴포넌트를 쓰지 않는다(P4)

두 제약을 모두 없애야 요구가 충족된다.

---

## 2. 해결 원칙

| # | 원칙 |
| --- | --- |
| 1 | **표현 모드와 데이터 필터를 분리한다.** 토글이 표현 모드를 정하고, 필터는 무엇을 담을지만 정한다. 두 축은 서로 간섭하지 않는다 |
| 2 | **스트립은 모든 모집 상태를 표현할 수 있어야 한다.** `개강확정`을 고르면 개강확정 카드가 나오듯 `마감`을 고르면 마감 카드가 나온다. 예외 없음 |
| 3 | **토글은 어떤 조건에서도 실효해야 한다.** 누르면 반드시 화면이 바뀐다 |
| 4 | 사업 요구 원문을 그대로 따른다: **"마감을 제외하지 않는다"** |
| 5 | **일관성의 정의를 고정한다: 형태(마크업 구조 · 배치 · 정보 항목)는 전 상태 동일, 차이는 라벨 텍스트와 색 토큰으로만 표현한다.** 마감 전용 구조를 만들지 않는다 |

### 2-1. 검토한 대안

| 안 | 판정 |
| --- | --- |
| 마감일 때 토글을 숨긴다 | **기각**. P1(표현 문법 불일치)이 그대로 남고, 컨트롤이 사라지는 것 자체가 새로운 혼란 |
| 마감일 때 토글 라벨을 다르게 쓴다 | **기각**. 죽은 컨트롤에 이름만 바꾸는 것 |
| **스트립이 마감 카드를 렌더하고 폴백을 제거한다** | **채택**. P1·P2를 동시에 없애고 규칙이 하나로 줄어든다 |

---

## 3. 실사로 확정된 사실 (구현 전 대조)

| # | 사실 | 확인 |
| --- | --- | --- |
| H1 | 일정 박스 = `.kium-schedbox` > `.kium-schedbox-head`(모드 헤더 + `.kium-schedbox-toggle`) > `.kium-schedbox-body` | 실측 |
| H2 | 스트립 카드 `.kium-scard2` / 리스트 `.kium-slist` > `.kium-srow[data-status]` | 실측 |
| H3 | 토글 라벨 2종: 접힘 `전체 일정 {n}개 회차` / 펼침 `간략히 보기` | 실측 |
| H4 | 스트립이 `effectiveStatus !== 'closed'`로 마감 제외 (F0-9) | `UpcomingSessionsStrip.tsx` |
| H5 | **`.kium-scard2[data-status="closed"]{opacity:.72}` CSS가 이미 존재하나 한 번도 렌더된 적 없음** (F0-11) | `styles/kium-open.css` |
| H6 | 리스트 마감 행 액션 `.kium-sact-closed` = 정적 배지 `마감` + 텍스트 링크 `다음 회차 상담`, 1행 유지 | 실측 |
| H7 | 마감 CTA 프리필(경로 B): `· 희망 회차: 10.21(수) ~ 22(목) · 2일 마감 → 다음 회차 문의` | 실측 |
| H8 | 현재 분포: 전체 20 / 모집중 8 / 개강확정 6 / 마감임박 5 / 마감 1 · 월별 10월 6 / 11월 7 / 12월 7 | 실측 |
| H9 | 전체 상태 스트립 첫 6장: 10.12 · 10.14 · 10.19 · 10.26 · 10.27 · 11.02 | 실측 |
| H10 | F22(0건 칩 미노출) · F23(`?preview` Empty 칩) 반영 완료 | 실측 |
| H11 | F24 폴백이 반영되어 마감 선택 시 `.kium-slist`가 스트립 자리에 렌더됨 | 실측 |
| H12 | 카드 자식 구조 = `.kium-scard2-date` → `.kium-scard2-days` → `button.kium-scard2-course` → `button.kium-sact` | 실측 |
| H13 | 공용 액션 = `button.kium-sact[data-tone]` > `span.kium-sact-st`(`svg` + `span.kium-sact-lb`) + `span.kium-sact-go`(CTA 라벨 + `svg`) | 실측 |
| H14 | 마감 액션 = `div.kium-sact-closed` > `span.kium-sbadge[data-tone="gray"]` + `button.kium-cta-next` (**구조가 다름**) | 실측 |
| H15 | 리스트 행 구조 = `li.kium-srow[data-status]` > `.kium-srow-date` / `.kium-srow-main` / `.kium-srow-meta`(시간·교육비) / `.kium-srow-act` | 실측 |

하나라도 다르면 **구현을 중단하고 불일치 내역을 보고**한다.

---

## 4. 변경 명세

### F27 (핵심) 스트립 소스 규칙 단일화

스트립 소스에서 **`closed` 제외 조건을 삭제**한다(H4 폐기).

```
변경 전: 미래 회차(end >= today) AND effectiveStatus !== 'closed' AND 필터 적용
변경 후: 미래 회차(end >= today) AND 필터 적용
```

- 정렬: `start ASC` (상태와 무관한 단일 날짜순). 상태별 우선순위 정렬 **금지**
- 미래 회차 조건(`end >= today`)은 **유지**한다. 스트립에 들어오는 `closed`는 "날짜는 남았으나 정원이 찬 회차"뿐이며, 날짜가 지나 자동 승격된 회차는 이 조건에서 이미 제외된다
- 노출 수 규칙(PC/TB 6장 · MO 3장), 스냅·peek, 필터 연동 갱신 **무변경**

**적용 결과 (모집 상태 전체 기준)**

| 순번 | 변경 전 (H9) | 변경 후 |
| --- | --- | --- |
| 1 | 10.12 마감임박 | 10.12 마감임박 |
| 2 | 10.14 개강확정 | 10.14 개강확정 |
| 3 | 10.19 모집중 | 10.19 모집중 |
| 4 | 10.26 마감임박 | **10.21 마감** |
| 5 | 10.27 모집중 | 10.26 마감임박 |
| 6 | 11.02 개강확정 | 10.27 모집중 |

> F21이 보존하려던 「MO 3장 3상태 커버리지」(10.12·10.14·10.19)는 그대로 유지된다. PC 6장에서 11.02가 밀려나지만 3상태 커버리지는 유지되며, 이는 검토 편의 항목이므로 사업 요구가 우선한다.

### F28 스트립 마감 카드 표현

`.kium-scard2`가 `closed`를 렌더할 수 있게 한다. **CSS는 이미 준비되어 있다**(H5).

**카드 구조는 전 상태 동일** (마감 전용 구조 신설 금지)

```html
<div class="kium-scard2" data-status="{status}">
  <p class="kium-scard2-date"><svg/><b>{날짜}</b></p>
  <p class="kium-scard2-days">{n}일 과정</p>
  <button class="kium-scard2-course">{과정명}</button>
  <!-- 액션: F31 참조 -->
</div>
```

| 요소 | 규격 |
| --- | --- |
| 카드 속성 | `data-status="closed"` 부여 → 기존 `opacity:.72` 자동 적용 |
| 자식 노드 구성 | **4요소 고정**: 날짜 / 일수 / 과정명 / 액션. 전 상태 동일 |
| 정보 항목 | 카드에는 교육시간·교육비를 **표시하지 않는다**(다른 상태 카드와 동일). 리스트 행의 `.kium-srow-meta`를 카드로 옮기지 말 것 |
| `aria-label` | 기존 카드 규격(`{과정명}, {날짜 풀텍스트}, {n}일 과정, {교육비} {상태} {CTA라벨}`) 그대로 |
| 카드 폭 | 스트립 카드가 1장뿐일 때도 폭을 늘리지 않는다. 기존 카드 폭 고정 + 좌측 정렬 |

### F31 액션 영역 형태 통일 (v1.1 신설 · P4 해소)

마감 액션을 **공용 액션 컴포넌트 `button.kium-sact` 하나로 통일**한다. 카드와 리스트 행 모두에 적용한다.

**변경 후 (전 상태 동일 구조)**

```html
<button class="kium-sact" data-tone="{tone}" aria-label="...">
  <span class="kium-sact-st"><svg/><span class="kium-sact-lb">{상태 라벨}</span></span>
  <span class="kium-sact-go">{CTA 라벨}<svg/></span>
</button>
```

| 상태 | `data-tone` | `.kium-sact-lb` | `.kium-sact-go` | 동작 |
| --- | --- | --- | --- | --- |
| 모집중 | 기존 값 | `모집중` | `상담하기` | 프리필 경로 A (무변경) |
| 개강확정 | 기존 값 | `개강확정` | `상담하기` | 프리필 경로 A (무변경) |
| 마감임박 | `red` | `마감임박` | `상담하기` | 프리필 경로 A (무변경) |
| **마감** | **`gray`** | **`마감`** | **`다음 회차 상담`** | **프리필 경로 B (기존 `.kium-cta-next` 핸들러 재사용, 문안 H7 무변경)** |

- 마감도 **액션 전체가 하나의 버튼**이다. 정적 배지 + 별도 텍스트 링크 조합(`.kium-sact-closed` + `.kium-cta-next`)을 카드·리스트 어디에도 쓰지 않는다
- `.kium-sact-closed` · `.kium-cta-next` 는 **삭제하지 말고 미참조 처리**하고, 완료 보고에 미참조 목록으로 남긴다
- `.kium-sbadge[data-tone="gray"]` 자체는 다른 용도(과정 카드 회차 레이어 등)에서 계속 쓰이므로 무변경
- CSS: `.kium-sact[data-tone="gray"]` 규칙이 없으면 기존 톤 규칙과 같은 패턴으로 1개만 추가한다. 신규 색 토큰 금지, 기존 gray 토큰 재사용
- 마감 액션 버튼은 **활성 컨트롤이다**(상담으로 이동). `disabled` 처리 금지
- 접근성: 마감 버튼 `aria-label` 은 다른 상태와 같은 문장 패턴을 쓰되 끝을 `마감 다음 회차 상담` 으로 맺는다

### F32 리스트 행 액션 동기화 (v1.1 신설)

- 리스트 행(`.kium-srow[data-status="closed"] .kium-srow-act`)의 액션도 F31의 `button.kium-sact` 로 교체한다
- 리스트 행 자체의 구조(`.kium-srow-date` / `.kium-srow-main` / `.kium-srow-meta` / `.kium-srow-act`)는 **무변경**. 교체 대상은 `.kium-srow-act` 내부뿐이다
- 결과: 카드에서든 리스트에서든, 어떤 상태든 회차 액션은 항상 동일한 한 개의 버튼이다

### F29 F24 폴백 제거, 토글 실효 복원

- `.kium-schedbox-body`의 **3분기 폴백(F24)을 삭제**하고 원래 2분기로 되돌린다

| 조건 | 렌더 |
| --- | --- |
| `visible.length === 0` | 빈 상태(F26 문구 유지) |
| 토글 접힘 | **스트립** (F27 소스) |
| 토글 펼침 | `.kium-slist` (필터 적용된 `visible`) |

- 토글은 **어떤 필터 조합에서도 스트립 ↔ 리스트를 실제로 전환**해야 한다. 라벨만 바뀌고 내용이 같은 상태가 0건이어야 한다
- F24가 추가한 폴백 전용 `aria-live` 안내(`조건에 맞는 회차 {n}건을 일정 목록으로 표시했습니다`)는 **삭제**한다. 토글 전환 안내는 기존 규칙 사용
- 토글 라벨 규칙(H3) 무변경

### F30 Empty Case 검토 칩 상시 노출 (게이트 해제)

이 배포본은 고객 화면이면서 **동시에 기획서**다(F21 §1). 0건 상태의 화면을 디자이너·개발자가 **URL을 몰라도** 확인할 수 있어야 한다.

**변경**

| 항목 | 변경 전 (F23) | 변경 후 |
| --- | --- | --- |
| 노출 조건 | `?preview` 쿼리가 있을 때만 | **상시 노출** (상수 제어) |
| 위치 | 모집 상태 칩 행 맨 끝 | **동일** (모집 상태 칩 행 맨 끝) |
| 라벨 | `Empty Case` | 동일 |
| 시각 구분 | 점선 테두리 `.kium-chip-review` | 동일 |
| 카운트 · `data-st` | 미표기 · 미부여 | 동일 |

> 사업 검토 표현인 **「마감임박 우측」**은 이 규칙에 포함된다. F22(0건 칩 미노출)로 기간 `11월`·`12월`에서는 마감 칩이 사라지므로, 「행 맨 끝」은 그 조건에서 실제로 마감임박 칩의 우측이 된다. 위치를 특정 칩에 고정하지 않고 **행의 마지막**으로 두는 편이 F22와 충돌하지 않는다.

**노출 제어 (실서비스 배포 전 제거 보장)**

```tsx
/* [F30] 검토용 칩 — 이 배포본은 기획서를 겸하므로 상시 노출한다.
   ★ 실서비스 오픈 전 반드시 false 로 변경할 것. false 면 DOM 자체가 생성되지 않으며,
     ?preview=cases · ?preview=badges 검토 경로는 그대로 살아 있다. */
const SHOW_REVIEW_CHIP = true;
```

- 렌더 조건: `SHOW_REVIEW_CHIP || reviewMode` (`reviewMode`는 F23의 `?preview` 판정 그대로)
- 상수는 **한 곳에만** 선언하고, 선언부 주석에 제거 시점과 방법을 명기한다
- `SHOW_REVIEW_CHIP = false` 시 `.kium-chip-review` **DOM 0건**이어야 한다 (`display:none` 금지)
- `?preview=cases` · `?preview=badges` 동작은 **무변경**으로 보존한다

**릴리즈 게이트 등록**

- 릴리즈 체크리스트에 다음 항목을 A등급으로 추가한다: `실서비스 배포 전 SHOW_REVIEW_CHIP=false 확인, Empty Case 칩 DOM 0건`

---

## 5. 금지 · 무변경 (회귀 확인만)

- 회차 데이터 전건 무변경 (`relead-r1` 시드 포함: `status` · 일자 · `id` · `courseId` · `displayMonth` · 배열 순서)
- `KIUM_STATUS_ORDER` · `KIUM_SESSION_META` · `effectiveStatus()` 무변경
- F22(0건 칩 미노출)와 선택 칩 자동 해제 `useEffect` 무변경
- F23의 `?preview` 쿼리 판정(`reviewMode`) · 칩 마크업 · `.kium-chip-review` 스타일 무변경 (F30은 **노출 조건만** 확장한다. 칩의 형태·라벨·위치 규칙은 그대로)
- F25(과정 카드 회차 레이어의 필터 연동 표기) 무변경
- F26(빈 상태 문구 `해당 조건의 회차가 없습니다.` + [필터 초기화]) 무변경
- 프리필 문안 2종 · 관심 영역 자동 선택 · 확인 배너 무변경
- 모드 헤더 문구 · 세그먼트 라벨과 카운트(`전체과정 19` / `공개교육 9`) 무변경
- 전체 보기에서 회차 요소 DOM 미생성 원칙 · 교육비 노출 분기 무변경
- 사업소개 탭 · FAQ · 리드 회수 블록 · `HomeInquiry` 무변경
- `seatsLeft` 추가 금지 · 신규 색 토큰 금지 · 신규 의존성 금지
- **마감 전용 카드 레이아웃·마감 전용 액션 구조 신설 금지.** 상태 차이는 `data-tone`·라벨 텍스트로만 표현한다
- 리스트 행의 교육시간·교육비(`.kium-srow-meta`)를 카드로 옮기지 말 것
- 마감 액션에 `disabled` 부여 금지 (상담 동선이 살아 있어야 한다)
- `.kium-sact-closed` · `.kium-cta-next` **삭제 금지** (미참조 처리만)
- 상태별 우선순위 정렬 신설 금지 (단일 날짜순)

---

## 6. 검증

### 6-1. 표현 일관성 (P1 해소)

| # | 조건 | 기대 |
| --- | --- | --- |
| K1 | 모집 상태 `모집중` · `개강확정` · `마감임박` · `마감` 각각 선택 | **4개 상태 모두 카드 스트립**으로 렌더 (`.kium-scard2` > 0, `.kium-srow` = 0) |
| K2 | `마감` 선택 | 카드 1장(10.21~22 Re-Lead), `data-status="closed"`, 배지 `마감`, CTA `다음 회차 상담` |
| K3 | 모집 상태 `전체` | 스트립 첫 6장이 §4 F27 표의 「변경 후」와 일치 |
| K4 | MO(375px) | 첫 3장이 10.12 · 10.14 · 10.19 (3상태 커버리지 유지) |
| K5 | 정렬 | 전 조합에서 `start ASC` 단일 기준, 상태별 그룹핑 없음 |
| K6 | **카드 구조 동일성** | 4개 상태 카드의 **자식 노드 클래스 시퀀스가 완전히 동일**해야 한다: `kium-scard2-date` → `kium-scard2-days` → `kium-scard2-course` → `kium-sact`. 마감 카드에만 있는 노드·없는 노드 0건 |
| K7 | 정보 항목 | 마감 카드에 교육시간·교육비 표기 없음(다른 카드와 동일) |
| K8 | 카드 폭 | 마감 1장만 노출될 때 카드 폭이 다른 상태 카드와 동일, 좌측 정렬 |

### 6-2. 토글 실효 (P2 해소)

| # | 조건 | 기대 |
| --- | --- | --- |
| T1 | `마감` 선택 후 토글 클릭 | 카드 → 리스트로 **화면이 실제 전환**(`.kium-scard2` 0 / `.kium-srow` 1) |
| T2 | T1에서 재클릭 | 리스트 → 카드로 복귀 |
| T3 | 4개 상태 × 접힘/펼침 8조합 | 모든 조합에서 접힘과 펼침의 DOM이 서로 다름. **라벨만 바뀌는 경우 0건** |
| T4 | 라벨 | 접힘 `전체 일정 {n}개 회차` / 펼침 `간략히 보기` (H3 유지) |
| T5 | F24 잔여물 | 폴백 분기 코드·폴백 전용 `aria-live` 문구 grep 0건 |

### 6-3. 액션 형태 통일 (F31 · F32)

| # | 항목 | 기대 |
| --- | --- | --- |
| S1 | 마감 카드 액션 | `button.kium-sact[data-tone="gray"]` **단일 버튼** 1개. `.kium-sact-closed` · `.kium-cta-next` 미사용 |
| S2 | 라벨 | `.kium-sact-lb` = `마감`, `.kium-sact-go` = `다음 회차 상담` |
| S3 | 구조 동일성 | 4개 상태 액션의 자식 노드 클래스 시퀀스가 동일: `kium-sact-st`(+`kium-sact-lb`) → `kium-sact-go` |
| S4 | 리스트 행 | `.kium-srow[data-status="closed"] .kium-srow-act` 내부도 `button.kium-sact` 로 교체됨 |
| S5 | 행 구조 | `.kium-srow` 자체 구조(`-date` / `-main` / `-meta` / `-act`)는 무변경 |
| S6 | 동작 | 마감 액션 클릭 시 프리필 경로 B, 문안 H7과 문자열 일치 |
| S7 | 미참조 처리 | `.kium-sact-closed` · `.kium-cta-next` 는 삭제되지 않고 **미참조** 상태 (미참조 목록 보고) |
| S8 | 활성 상태 | 마감 액션 버튼에 `disabled` 미부여, 키보드 포커스·엔터 동작 정상 |
| S9 | `aria-label` | 다른 상태와 동일 문장 패턴, 끝이 `마감 다음 회차 상담` |

### 6-4. 마감 카드 품질

| # | 항목 | 기대 |
| --- | --- | --- |
| Q1 | CTA 프리필 | 경로 B 도달, 문안 H7과 문자열 일치 |
| Q2 | 리스트 행과 동일성 | 같은 회차를 카드/리스트에서 각각 눌렀을 때 프리필 결과가 동일 |
| Q3 | 레이아웃 | 마감 카드가 다른 상태 카드와 동일 높이·정렬, CTA 1행 유지 |
| Q4 | **대비 실측** | `opacity:.72` 상태에서 마감 배지·날짜 텍스트가 **AA(4.5:1)** 충족 여부. 미달 시 임의 수정하지 말고 **수치와 함께 보고** |

### 6-5. Empty Case 칩 (F30)

| # | 조건 | 기대 |
| --- | --- | --- |
| E1 | 일반 URL(`?preview` 없음) | 모집 상태 칩 행 **맨 끝**에 `Empty Case` 칩 1개 노출 |
| E2 | 기간 `11월` 선택 | 마감 칩이 사라지므로 `Empty Case` 칩이 **마감임박 칩 우측**에 위치 |
| E3 | 칩 선택 | `해당 조건의 회차가 없습니다.` + [필터 초기화] |
| E4 | 해제 | [필터 초기화] → 전체 복귀 |
| E5 | 시각 구분 | 점선 테두리 유지, 카운트 미표기, `data-st` 미부여 |
| E6 | 접근성 | `aria-label`에 검토용임이 명시, `aria-pressed` 갱신 |
| E7 | **제거 검증** | `SHOW_REVIEW_CHIP = false` 로 바꾸면 `.kium-chip-review` **DOM 0건** (확인 후 `true` 원복) |
| E8 | 검토 경로 보존 | `SHOW_REVIEW_CHIP = false` 상태에서도 `?preview=cases` · `?preview=badges` 로 칩 노출 |
| E9 | 가로 넘침 | 칩이 1개 늘어난 상태에서 320/375/768/1024/1440 넘침 0px |

### 6-6. 빈 상태

| # | 조건 | 기대 |
| --- | --- | --- |
| N1 | `Empty Case` 칩 선택 | `해당 조건의 회차가 없습니다.` + [필터 초기화] (F26 유지) |
| N2 | 일반 필터 조합 | 폴백이 사라졌으므로 실제 0건 조합에서 빈 상태 정상 노출 |

### 6-7. 회귀

| # | 항목 | 조치 |
| --- | --- | --- |
| R1 | `verify-btype.mjs` `I6`(마감 스트립 0장) | ★ **폐기**. `마감 선택 시 스트립 카드 1장`으로 기대값 교체 |
| R2 | F24에서 추가했던 폴백 단언(A2·A4 계열) | ★ **교체**. 카드 기준 단언으로 갱신 |
| R3 | `verify-btype2.mjs` `Q8`(모집 상태 칩) · `P4`(마감 프리필) | 무변경. 깨지면 회귀 |
| R4 | 신규 단언 | K1 · K2 · K6 · T1 · T3 · S1 · S3 · S4 · E1 · E7 **10건** 추가 (기존 단언 삭제 금지, 교체·추가만) |

- 타 페이지 5경로 회귀 0 · `npm run build` 경고 0 · `tsc --noEmit` 0 · 콘솔 에러 0 · 신규 의존성 0
- 320 / 375 / 768 / 1024 / 1440 가로 넘침 0px

---

## 7. 완료 보고 양식

1. §3 H1~H11 대조 결과
2. 변경 파일 목록 및 diff 요약 (**F24 폴백 삭제분을 명시**)
3. §6-1 K1~K5
4. §6-2 T1~T5 (**T3은 8조합 표를 그대로**)
5. §6-3 Q1~Q4 (**Q4는 대비 수치를 그대로**)
6. §6-4 E1~E9 (**E7은 false 전환 시 DOM 카운트 원문**)
7. §6-5 N1~N2
8. §6-6 회귀 결과 및 교체·추가한 단언 목록 (항목명 · 이전 기대값 → 새 기대값)
9. `npm run build` · `tsc --noEmit` 결과
10. 스크린샷: 모집 상태 전체 / 모집중 / 개강확정 / 마감임박 / 마감 각 접힘·펼침, `Empty Case` 칩 노출 및 선택 화면, PC·MO
11. 명세와 달리 판단한 부분과 사유
